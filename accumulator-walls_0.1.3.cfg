[entity-name]
  accumulator-wall=光电储电墙
  accumulator-interface=光电储电墙

[recipe-name]
  accumulator-wall=光电储电墙

[technology-name]
  accumulator-wall=光电储电墙

[technology-description]
  accumulator-wall=含有储电池的墙

[mod-setting-name]
  accumulator-wall-capacity=蓄能器壁容量\n Accumulator Wall capacity
  accumulator-wall-recipe-ratio=累加器壁配方比\n Accumulator Wall recipe ratio
  accumulator-wall-stack-size=壁堆叠尺寸\n Wall stack size
  accumulator-wall-max-health=累加器墙的健康状况\n Health of Accumulator Wall

[mod-setting-description]
  accumulator-wall-capacity=蓄能器墙存储的MJ值，默认值为5，最小值1，最大值150\n Value in MJ stores by the Accumulator Wall, default is 5, min 1, max 150
  accumulator-wall-recipe-ratio=配方的比率，例如：N=10->1蓄能器+10石墙=10蓄能壁\n Ratio for the recipe, e.g.: N = 10 -> 1 Accumulator + 10 Stone wall = 10 Accumulator Walls
  accumulator-wall-stack-size=墙堆栈大小，默认为100，可以在区间[11000]\n Wall stack size, default is 100, can be in the interval [1, 1000]
  accumulator-wall-max-health=太阳能墙的健康值，默认值为350，可以在区间[110000]内\n Health of the Solar Wall, default is 350, can be in the interval [1, 10000]

[mod-name]
accumulator-walls=光电储电墙
[mod-description]
accumulator-walls=含有储电池的墙
  